version: '3'

services:
  flask_app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - /mnt/k2/aqpi/XBand/web-files/PRODUCTS/Raingauge_Comparison:/app/Raingauge_Comparison
    command: python api.py  
